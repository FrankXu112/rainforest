<h1>All Products</h1>

<ul class="index">
	<% @products.each do |product| %>
	  <li><%= link_to "#{product.name}-#{product.formatted_price}", product_path(product) %>
  			<%= image_tag product.url %>
	  </li>
	<% end %>
</ul>