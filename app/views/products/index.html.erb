<h1>All Products</h1>

<ul>
	<% @products.each do |product| %>
	  <li><%= link_to "#{product.name}-#{product.formatted_price}", "/products/#{product.id}" %>
	  <%= product.description %>
	  </li>
	<% end %>
</ul>